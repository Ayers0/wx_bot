# OpenAI-compatible API key for config.json ai.models.*.api_key_env
# Copy this file to .env only for local shell helpers if needed.
# The bot itself reads environment variables from the process environment.
OPENAI_COMPAT_API_KEY=replace-with-your-api-key